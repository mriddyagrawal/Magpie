# Which LLM provider to use. Valid: moonshot | openrouter | local
# (Provider-specific keys below; only the active provider's key needs to be set.)
LLM_PROVIDER=moonshot

# --- Moonshot Kimi (used when LLM_PROVIDER=moonshot) ---
MOONSHOT_API_KEY=sk-your-key-here
MOONSHOT_MODEL=kimi-k2.5
MOONSHOT_BASE_URL=https://api.moonshot.ai/v1

# --- OpenRouter (used when LLM_PROVIDER=openrouter) ---
OPENROUTER_API_KEY=sk-or-v1-your-key-here
OPENROUTER_MODEL=google/gemma-4-26b-a4b-it:free
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# --- Local LLM via llama-server subprocess (LLM_PROVIDER=local) ---
# Subprocess-based local inference using llama.cpp's `llama-server`
# binary. The Python sidecar manages a pool of subprocesses (one per
# active model profile), spawning + health-checking + LRU-evicting as
# needed. See Specs/llama_server_migration.md.
#
# After `just sync-environment`, run `just install-llama-server` to
# download the right binary for this platform from llama.cpp's GitHub
# releases. macOS quarantine is stripped automatically.
#
# Model selection: `LOCAL_MODEL` is the HF GGUF *repo*; `LOCAL_QUANT`
# selects the specific file inside the repo. Defaults pull LFM2.5-VL-3B
# at Q6_K (2.22 GB) — effectively lossless against Q8_0 at this size.
# Smaller: Q4_K_M (1.67 GB), Q5_K_M (1.94 GB). Larger: Q8_0 (2.87 GB).
# Quant names are repo-specific; a value not in the repo 404s at first
# inference. Any new repo also needs a filename convention added to
# _REPO_PATTERNS in src/inference/model_downloader.py.
LOCAL_MODEL=LiquidAI/LFM2.5-VL-3B-GGUF
LOCAL_QUANT=Q6_K
# Context window in tokens. Bigger = more KV-cache RAM. 16384 fits Magpie's
# T3 / answer prompts comfortably; raise if your answer step needs longer
# document slices.
LOCAL_N_CTX=16384
# Sampling temperature. 0.0 = deterministic (good for structured-output
# T3 / rewrite). 0.7 = default for the answer step. /generate callers
# can override per-request.
LOCAL_TEMPERATURE=0.7

# --- llama-server pool / binary settings ---
# Path to the llama-server binary. Empty = auto-discover (env →
# <APP_DATA_DIR>/bin/llama-server → next to sys.argv[0]/bin → PATH).
LLAMA_SERVER_PATH=
# Minimum llama.cpp release tag we'll accept. Hard-fails at startup if
# the installed binary is older. Bump when adopting newer features.
LLAMA_SERVER_MIN_VERSION=b10502
# First port the pool tries when spawning a subprocess. NOT 8765 —
# that's the FastAPI sidecar. Pool walks forward (9100, 9101, ...)
# until it finds one not bound.
LLAMA_SERVER_BASE_PORT=9100
# How many subprocesses can be live concurrently. 1 = sequential
# (LRU eviction on each new profile). Bump on RAM-rich dev boxes
# where keeping text + vision both warm is worth ~3 GB extra.
LLAMA_SERVER_MAX_LOADED_MODELS=1
# Unload a subprocess after this many seconds of inactivity. Frees
# the model's RAM without losing the binary discovery / port state.
# Set to 0 to disable.
LLAMA_SERVER_IDLE_TIMEOUT_S=600
# Health-check timeout when spawning. Larger models load slower;
# this should accommodate the slowest profile + a margin.
LLAMA_SERVER_STARTUP_TIMEOUT_S=60
# Per-profile model selection. Profile names live in src/inference/profiles.py.
# DEFAULT is the vision-capable profile — LFM2.5-VL + mmproj together. The
# projector is loaded once and idles (zero inference cost) on text-only
# calls, so one subprocess serves every request without LRU swaps.
# There is no text-only profile. `--mmproj` is a spawn-time flag, so a
# projector cannot be attached to or detached from a running process, and
# an idle projector costs no inference time — only 0.54 GB resident.
# Saving that would mean a full cold reload on every text<->image switch.
LLAMA_SERVER_TEXT_MODEL=lfm25-vl-vision
# `text-only` is the other registered profile: the same LOCAL_MODEL/LOCAL_QUANT
# weights with no projector, for pure-text readers (Qwen2.5-*-Instruct-GGUF,
# LFM2.5-1.2B-Instruct-GGUF; see _REPO_PATTERNS). Used by the eyes-vs-brain
# reader arms; images then route to LLAMA_SERVER_VISION_MODEL below.
# Vision profile used as the fallback target when a text-bound caller
# hits an image (only relevant if you opted out of the unified default
# above). Set to empty to disable local vision entirely — image bytes
# then silently fall through to a text-only summary.
LLAMA_SERVER_VISION_MODEL=lfm25-vl-vision
# mmproj projector variant for the default vision profile. LFM2.5-VL
# ships only Q8_0 (0.54 GB), F16 and BF16 (0.80 GB each) — a NARROWER set
# than the model's own quants, so a value valid for LOCAL_QUANT can still
# 404 here. Q8_0 is the default: no measurable vision-quality cost at 3B.
LOCAL_MMPROJ_VARIANT=Q8_0
# Set to 1 to mirror EVERY llama-server stderr line to the sidecar's
# stderr. Default (unset / 0) shows only high-signal lines (errors,
# warnings, "model loaded", "server is listening") so `just sync`
# walker output stays readable. Crash-tail buffer always keeps the
# last 50 lines regardless, so spawn failures still report fully.
LLAMA_SERVER_VERBOSE=

# --- Pipeline knobs ---
# Default for the /query endpoint's `rewrite` flag when the client doesn't
# specify it. true = enable Kimi-style query expansion before retrieval
# (~20s extra LLM round-trip; sharper retrieval on noisy questions).
# false = send the raw question to the embedder. Explicit per-request
# value still wins. The Tauri window UI doesn't expose a toggle today,
# so this env knob is the de-facto global setting for it.
REWRITE=false

# --- Qdrant ---
# Magpie talks to a Qdrant Rust binary running on localhost — no env
# config needed in the common case. Set up with:
#   just qdrant-install   # one-time download (~30 MB)
#   just qdrant-up        # start the binary on port 6433
# Port 6433 (NOT Qdrant's default 6333) avoids colliding with OpenWhispr
# and other apps that ship their own Qdrant. Remote clusters and the
# embedded Python shim are both unsupported by design — Magpie is
# local-first and the index never leaves your machine.

HF_TOKEN=your-hf-token-here

# ---------------------------------------------------------------------------
# Answer-step speed (local provider) — added 2026-08-28
# ---------------------------------------------------------------------------
# Prefix cache: the answer prompt now leads with the retrieved files, and the
# processed state for that prefix is saved to disk and restored on the next
# question that uses the same files (20-60 ms instead of re-reading them).
# Slot files are ~16 KB per token (~100 MB for a 6K-token document), so the
# cache is a plain LRU capped at MAGPIE_KV_CACHE_MB. MAGPIE_KV_CACHE=0 turns
# it off; MAGPIE_KV_SLOT_DIR moves it off a slow or small data disk.
MAGPIE_KV_CACHE=1
MAGPIE_KV_CACHE_MB=2048
MAGPIE_KV_SLOT_DIR=
# Prompt order. auto = question first on a cold ask (the 3B reads better when
# it knows the question; sem6 lost 5/40 reading blind), files first only when
# the slot for exactly those files is already on disk (a repeat question, where
# the restore saves 1-3 s); the slot is built in the background after a cold
# answer. `question` or `files` pins one order for an eval arm.
MAGPIE_PROMPT_ORDER=auto

# Extractive fast path: factoid questions ("how many", "when", "who", "what
# is the <rate|date|...>") against one or two files are answered by a small
# span-extraction model that copies the exact phrase out of the file — a few
# hundred ms on CPU, and it cannot invent. Off by default until the eval
# gate in Evaluations/phyll/REPORT.md is met. MIN_SCORE is the reader's
# confidence over "no answer" (0..1); below it the 3B reader answers instead.
MAGPIE_EXTRACTIVE=0
MAGPIE_EXTRACTIVE_MIN_SCORE=0.5
MAGPIE_EXTRACTIVE_MODEL=deepset/tinyroberta-squad2

# Index-time transcripts for pixels-only files (scanned PDFs AND standalone
# images since 2026-08-29): the answer step reads the transcript as text and
# never sends pixels to the model when one exists. Written by
# Evaluations/transcribe_index.py (backend ocr = RapidOCR on CPU, needs
# `uv sync --extra ocr`; backend vlm = the local vision model). Default folder
# is <APP_DATA_DIR>/transcripts; override to keep two backends side by side.
MAGPIE_TRANSCRIPTS_DIR=
# Pages transcribed per file (receipts are 1-3; keeps a scanned booklet cheap).
MAGPIE_TRANSCRIBE_MAX_PAGES=8
