{
  "config_name": "topk2-rerank-off",
  "dataset": "receipts",
  "params": {
    "model_config": "lfm-local",
    "prompt_style": "baseline",
    "temperature": 0.0,
    "solo_margin": 0,
    "rerank": false,
    "local_n_ctx": 16384,
    "top_k": 2,
    "top_k_retrieval_max": 12,
    "rewrite": true,
    "enumerate_lists": true,
    "fast_search": true,
    "index_fast_tier": true,
    "index_summary_tier": true,
    "llama_startup_timeout_s": 300,
    "memory": null,
    "_notes": {
      "top_k": "owner decision 2026-08-29: k=2, between H3's k=1 and k=3 points. One knob for both axes - worker.py passes top_k straight into ask(), so 2 files are retrieved AND 2 are fed to the generator. top_k_retrieval_max stays 12 (retrieval-only sweeps only; unused by this run)",
      "rerank": "MAGPIE_RERANK kill-switch; production default is ON - this run deliberately diverges (owner decision 2026-08-29, same rationale as baseline): the cross-encoder scores visual hits against the constant placeholder '(visual match - page N)' (product bug #1), so rerank off = ColQwen's own fusion ranking. false ALSO structurally disables the solo gate (margin is on cross-encoder score scale); run.json stamps solo_gate_structurally_off",
      "solo_margin": "0 = gate off, stated explicitly rather than implied by rerank=false (owner decision 2026-08-29); production default is 2.0",
      "fast_search": "production ships visual-tier search OFF; receipts are scanned images and run.py:96 hard-fails a visual_tier-required dataset without it. Dataset-level override (review #31), worker default stays production-faithful False",
      "temperature": "evals run at 0 (PLAN 4.2); production settings.json default is 0.7",
      "local_n_ctx": "pinned: .env.example fresh-clone value & shipped-beta ctx; this machine's .env says 65536 and must not leak (PLAN 3)"
    }
  }
}
