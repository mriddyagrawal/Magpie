Phases ran pre-6b085ac (isolation recorded as `cache_unchanged`, not yet
enforced; both values True, verified). Scoring re-ran post-fix — see
metrics.json enriched_at_sha. Mixed provenance is recorded, not hidden.
