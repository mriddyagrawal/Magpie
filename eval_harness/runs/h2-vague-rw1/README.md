Retrieval-only H2 arm (PLAN docket H2). The 2x2's fourth cell — primary
phrasing, rewrite ON — is supplied by the baseline run's retrieve pass
(20260828T081416Z-receipts-baseline: same corpus, index hash, k_max=12,
rewrite=true), not by a folder here (#77). Vague-arm golden lives outside
the repo datasets dir (variant questions derived 1:1 from
datasets/receipts/golden.json question_variants).
